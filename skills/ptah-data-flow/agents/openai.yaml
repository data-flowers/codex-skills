interface:
  display_name: "Ptah Data Flow"
  short_description: "Curate, publish, and deploy Ptah data"
  default_prompt: "Use $ptah-data-flow to curate and publish the current Ptah data, using narrow single-writer updates after the initial verified publish."
